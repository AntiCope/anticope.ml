const {title} = require("./metadata")

module.exports = {
    name: title,
    short_name: title,
    start_url: '/',
    background_color: '#030c15',
    theme_color: '#8fbcbb',
    display: 'standalone',
    lang: 'en',
    icon: 'src/images/icon.svg',
}